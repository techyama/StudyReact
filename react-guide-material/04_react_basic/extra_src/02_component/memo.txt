コンポーネントについて
→画面の各構成要素をReactで定義したもの

メリット
・再利用性の向上(コードが使いまわせる)
・可読性の向上(コードが整理される)
・疎結合になる(バグを減らせる)
例:コンポーネントごとにテストできる

定義
function Welcome() {
    return <h1>Hello</h1>;
}
必ず先頭は大文字である必要がある(babelが読み込むのは大文字のため)

コンポーネントの実行
<Welcome />